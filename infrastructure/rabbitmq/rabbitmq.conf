# RabbitMQ configuration
# Exchanges, queues, and bindings are declared by each service on connect
# via Spring AMQP RabbitAdmin (Java services) and aio-pika (Python services).
# Do NOT use management.load_definitions here — it replaces all entities on
# boot, which deletes the RABBITMQ_DEFAULT_USER/PASS credentials.

log.console = true
log.console.level = info
