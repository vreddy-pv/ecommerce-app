spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/analytics_test_db
    username: test_user
    password: test_password
  jpa:
    hibernate:
      ddl-auto: create-drop
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
